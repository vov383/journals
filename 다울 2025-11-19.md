---
title: 다울 2025-11-19
created: 2025-11-19 10:11
aliases: 
tags: [다울일간노트]
---


Yesterday: [[다울 2025-11-18]] 
Tomorrow: [[다울 2025-11-20]] 


## 오늘 작업 목표

[[예상질문 10개 선별]]
[[temp/예상질문 목록|예상질문 목록]]
[[스마트제조25_발표예상질문과답변]]

[[노트북lm 예상질문 20선]]
[[노트북lm 내용에 없지만 제시될 수 있는 예상질문 20선]]

TB_COMEOUT_TREND 테이블 컬럼명 변경

```SQL
ALTER TABLE `tb_comeout_trend`
	CHANGE COLUMN `BIN1` `BIN_LV1` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `TR_SEQ`,
	CHANGE COLUMN `BIN2` `BIN_LV2` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV1`,
	CHANGE COLUMN `BIN3` `BIN_LV3` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV2`,
	CHANGE COLUMN `BIN4` `BIN_LV4` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV3`,
	CHANGE COLUMN `BIN5` `BIN_LV5` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV4`,
	CHANGE COLUMN `BIN6` `BIN_LV6` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV5`,
	CHANGE COLUMN `BIN7` `BIN_LV7` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV6`,
	CHANGE COLUMN `BIN8` `BIN_LV8` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV7`,
	CHANGE COLUMN `BIN9` `BIN_LV9` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV8`,
	CHANGE COLUMN `BIN10` `BIN_LV10` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV9`,
	CHANGE COLUMN `BIN11` `BIN_LV11` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV10`,
	CHANGE COLUMN `BIN12` `BIN_LV12` FLOAT(5,2) NULL DEFAULT NULL COMMENT 'BIN레벨(%)' AFTER `BIN_LV11`;
```

## Not done yet

```dataviewjs

dv.taskList(dv.pages('-"3. Resource/Templates"').file.tasks

.where(t => !t.completed && !t.text.includes("@frank") &&

!t.text.includes("#task")

))

```

---

## 배운 것




## 정리가 필요한 메모는 아래에


###### 컴퓨터 과학의 근본 원리 현재 기술의 토대와 미래 발전 방향성 분석
![[journals/temp/컴퓨터 과학의 근본 원리 현재 기술의 토대와 미래 발전 방향성 분석]]
